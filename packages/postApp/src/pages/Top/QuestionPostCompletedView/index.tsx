import { css } from '@emotion/css';
import { FC } from 'react';

export const QuestionPostCompletedView: FC = () => {
  return <div className={baseWrapper}>問題の登録が完了しました。</div>;
};

const baseWrapper = css``;
